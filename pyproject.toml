[project]
name = "QST"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "ipython",
    "ipykernel",
    "ipywidgets",
    "matplotlib",
    "numpy",
    "qutip",
    "scipy",
    "tqdm",
    "line-profiler>=5.0.2",
    "threadpoolctl>=3.6.0",
]
